#include<bits/stdc++.h>
using namespace std;
int main()
{
    long long n,k,i,j,k,l,s;
    vector<long long>v;
    vector<long long>::iterator it;
    cin>>n>>k;
    for(i=0;i<n;i++){
        cin>>a[i];
        s+=a[i];
    }
    for(i=0;i<n;i++){
            j=s-a[i];
        if(j%k==0&&a[i]<(j/k)){

        }
    }
    return 0;
}
